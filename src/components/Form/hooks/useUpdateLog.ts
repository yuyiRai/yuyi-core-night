// import * as React from 'react';
export function useLog(...msg: any[]) {
  // const a = React.useRef(0)
  // React.useDebugValue(msg[0])
  // React.useEffect(() => {
  //   if (a.current > 1)
  //     console.log(...msg);
  //   a.current++
  // }, [a.current])
}
import { observable, observe, computed, action, intercept,set as obsSet, get as obsGet} from 'mobx'
import { observer } from 'mobx-react'
export {
  observable, observe, computed, action, intercept, obsSet, obsGet,
  observer
}